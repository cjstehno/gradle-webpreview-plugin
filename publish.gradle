plugins {
    id "org.ajoberstar.github-pages" version "1.6.0"
}

githubPages {
    repoUri = 'git@github.com:cjstehno/gradle-webpreview-plugin'
    pages {
        from(file('build/site')) {
            into '.'
        }
    }
}